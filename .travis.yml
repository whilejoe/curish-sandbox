language: node_js
node_js:
   - 6
cache:
  directories:
    - node_modules
script:
  - yarn test
  - yarn build
before_deploy: "echo 'BEFORE FIREBASE DEPLOY'"
deploy:
  provider: firebase
  token:
    secure: $FIREBASE_DEPLOY_TOKEN