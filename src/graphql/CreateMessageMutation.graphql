mutation CreateMessageMutation($text: String!, $chatId: ID!, $fromId: ID!) {
  createMessage(text: $text, fromId: $fromId) {
    id
    text
    createdAt
    from {
      id
      name
    }
  }
}

# chat: MessagechatChat
