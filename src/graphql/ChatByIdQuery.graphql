query ChatByIdQuery($id: ID!) {
  Chat(id: $id) {
    id
    createdAt
    updatedAt
    users {
      id
      userName
    }
    messages(last: 40) {
      id
      createdAt
      text
      from {
        id
        userName
      }
    }
  }
}
