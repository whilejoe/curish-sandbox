import styled from 'styled-components';

const StoryContainer = styled.div`
  position: relative;
  max-width: 44rem;
  margin-right: auto;
  margin-left: auto;
  padding-top: 0.8rem;
  padding-right: 1rem;
  padding-left: 1rem;
`;

export default StoryContainer;
